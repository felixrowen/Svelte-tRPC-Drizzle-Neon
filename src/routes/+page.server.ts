import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
  // Server-side data can be loaded here if needed
  return {
    // You can pass data to the page here
  };
};
